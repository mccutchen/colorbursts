$(function() {

    // $(window).scroll(function(e) {
    //     if (!loading && document.body.scrollTop == $(document).height() - $(window).height()) {
    //        fetchBursts();
    //     }
    // });
});
